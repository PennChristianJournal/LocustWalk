h2.strong: a(href='/archive') Archive

ul.clean
  each feature, index in features
    li: a(href=`/articles/${feature.slug}`) #{moment(feature.date).format('MMM YYYY')}
  //- br
  //- li: a(href='/archive') &#8212; See more